# Valkey-Server 典型配置文件（valkey.conf）

# 说明：所有配置项均标注推荐值及用途，# 后为注释，可根据实际环境调整

# 核心原则：兼顾安全性、可靠性与性能，适配中小规模生产环境（单机 / 主从架构）

\##############################################################################

# 一、基础网络配置（控制服务访问范围）

\##############################################################################

# 绑定 IP：生产环境建议指定信任 IP（如内网段），避免 0.0.0.0（允许所有 IP 访问）

bind 192.168.1.100 127.0.0.1

# 服务端口：默认 6379，若端口冲突可修改（如 6380）

port 6379

# 监听队列长度：默认 511，高并发场景可调整为 1024

tcp-backlog 1024

# 连接超时时间：单位秒，默认 0（无超时），建议设为 300（5 分钟）避免闲置连接占用资源

timeout 300

# TCP 保活：探测闲置连接是否有效，单位秒（建议 60 秒探测一次）

tcp-keepalive 60

\##############################################################################

# 二、安全配置（防止未授权访问与误操作）

\##############################################################################

# 保护模式：开启时仅允许本地连接或已认证连接，生产环境必须开启

protected-mode yes

# 访问密码：强密码（建议 8 位以上，包含字母、数字、特殊符号），避免明文硬编码（可通过环境变量注入）

requirepass V@lkey2025\_Prod

# 命令重命名：禁用高危命令（如 FLUSHDB/FLUSHALL），或重命名为复杂名称

rename-command FLUSHDB ""

rename-command FLUSHALL ""

rename-command CONFIG "Valkey\_Config\_2025"

# 最大客户端连接数：默认 10000，根据服务器资源调整（建议不超过 5000，避免内存溢出）

maxclients 5000

\##############################################################################

# 三、持久化配置（保证数据不丢失，平衡性能与可靠性）

\##############################################################################

# 1. RDB 持久化（快照模式，适合全量备份）

# 触发条件：900 秒内修改 1 个键、300 秒内修改 10 个键、60 秒内修改 10000 个键（满足任一即触发）

save 900 1

save 300 10

save 60 10000

# RDB 文件压缩：开启（yes）节省磁盘空间，关闭（no）提升性能（建议开启）

rdbcompression yes

# RDB 文件校验：开启（yes）确保文件完整性，关闭（no）提升写入速度（建议开启）

rdbchecksum yes

# RDB 文件路径与名称：默认 dump.rdb，建议指定明确路径（需确保目录存在且有权限）

dir /var/lib/valkey/

dbfilename dump-6379.rdb

# 2. AOF 持久化（Append-Only File，适合增量备份，建议与 RDB 配合使用）

# 开启 AOF：生产环境必须开启（yes），仅用 RDB 风险较高

appendonly yes

# AOF 文件名称：默认 appendonly.aof，建议带端口号区分多实例

appendfilename appendonly-6379.aof

# AOF 刷盘策略：

# everysec（每秒刷盘，平衡性能与可靠性，推荐）、always（每次写都刷盘，性能低）、no（由系统决定，可靠性低）

appendfsync everysec

# AOF 重写触发条件：文件体积达到当前 100%（即翻倍）且大于 64MB 时触发（避免频繁重写）

auto-aof-rewrite-percentage 100

auto-aof-rewrite-min-size 64mb

# AOF 文件损坏修复：开启（yes），启动时自动修复损坏的 AOF 文件（建议开启）

aof-load-truncated yes

# AOF 与 RDB 混合持久化：开启（yes），重写时同时包含 RDB 全量数据和 AOF 增量数据（推荐）

aof-use-rdb-preamble yes

\##############################################################################

# 四、内存优化配置（避免内存溢出，提升资源利用率）

\##############################################################################

# 最大内存限制：根据服务器内存配置（如服务器 16GB 内存，建议设为 12GB，留 4GB 给系统）

maxmemory 12gb

# 内存淘汰策略：内存满时删除键的规则（生产环境推荐以下两种）

# allkeys-lru（删除所有键中最近最少使用的，适合通用场景）

# volatile-lru（仅删除带过期时间的键中最近最少使用的，适合有持久化需求的场景）

maxmemory-policy allkeys-lru

# 内存淘汰采样数：默认 5，数值越大越精准但性能消耗高（建议 5-10）

maxmemory-samples 5

# 过期键删除策略：主动删除 + 惰性删除结合（默认配置，无需修改）

lazyfree-lazy-eviction yes

lazyfree-lazy-expire yes

lazyfree-lazy-server-del yes

\##############################################################################

# 五、服务运行配置（控制服务启动方式与日志）

\##############################################################################

# 守护进程模式：后台运行（yes，生产环境必须开启），前台运行（no，仅用于调试）

daemonize yes

# 进程 PID 文件：记录服务进程 ID，用于启停管理（需确保目录存在）

pidfile /var/run/valkey/valkey-6379.pid

# 日志级别：debug（调试）、verbose（详细）、notice（默认，推荐）、warning（警告）

loglevel notice

# 日志文件路径：默认标准输出，生产环境必须指定文件（需确保目录存在且有权限）

logfile "/var/log/valkey/valkey-6379.log"

# 数据库数量：默认 16 个，无需多实例时可减少（如设为 4）

databases 4

\##############################################################################

# 六、主从复制配置（仅从节点需配置，主节点无需修改）

\##############################################################################

# 主节点 IP 与端口：从节点连接的主节点地址（需替换为实际主节点信息）

slaveof 192.168.1.101 6379

# 主节点密码：若主节点配置了 requirepass，从节点必须填写（与主节点密码一致）

masterauth V@lkey2025\_Prod

# 从节点只读：开启（yes），避免从节点被误写（生产环境必须开启）

slave-read-only yes

# 主从复制积压缓冲区大小：默认 1mb，网络不稳定场景可增大（如 16mb）

repl-backlog-size 16mb

# 主从复制超时时间：默认 60 秒，网络差时可延长（如 120 秒）

repl-timeout 120

\##############################################################################

# 七、高级性能优化（根据服务器硬件调整）

\##############################################################################

# IO 线程数：默认 1（单线程），Redis 6 + 支持多 IO 线程（Valkey 继承该特性），建议设为 CPU 核心数的 1/2（如 4 核 CPU 设为 2）

io-threads 2

# 多 IO 线程是否处理读命令：开启（yes），提升读性能（推荐）

io-threads-do-reads yes

# 哈希表负载因子：默认 1，数值越小哈希冲突越少但内存占用越高（建议 0.7-1.0）

hash-max-ziplist-entries 512

hash-max-ziplist-value 64

# 列表压缩阈值：元素个数≤512 且每个元素≤64 字节时用压缩存储（提升内存效率）

list-max-ziplist-size -2

list-compress-depth 0

> （注：文档部分内容可能由 AI 生成）